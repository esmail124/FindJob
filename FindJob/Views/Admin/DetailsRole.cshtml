@*@using TripPJ.UI.Dtos.Admin;
@model IEnumerable<TripPJ.Model.Models.ApplicationRole>

<h3>Details Role</h3>

<table class="table table-bordered table-striped">
    <tr>
        <td>Id</td>
        <td>Name</td>
        <td>Gender</td>
        <td>Date Of Birth</td>
        <td>Phone Number</td>
        <td>Address</td>
        <td>Email</td>
        <td>Password</td>
        <td>CreateTime</td>
        <td>UpdateTime</td>
        <td>Role Check</td>
    </tr>
    @if (!(Model is null))
    {
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>
                <td>@item.Gender</td>
                <td>@item.DateOfBirth</td>
                <td>@item.PhoneNumber</td>
                <td>@item.Address</td>
                <td>@item.Email</td>
                <td>@item.Password</td>
                <td>@item.CreateTime</td>
                <td>@item.UpdateTime</td>
                <td>
                    <div>
                        <form method="post" href="~/Admin/DetailsRole">
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" value="id" name="id" id="flexCheckChecked" checked>
                                <label class="form-check-label" for="flexCheckChecked">
                                    Is In Role?
                                </label>
                            </div>
                        </form>
                    </div>
                </td>
            </tr>
        }
    }
    else
    {
        <tr>
                    <td>there is no user in this role yet </td>
        </tr>
    }
    
</table>
<form method="post" >

</form>*@